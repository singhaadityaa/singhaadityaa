# Profile asset guidelines

Visual assets in `assets/` should support the profile rather than dominate it.

- Prefer SVG for diagrams and simple illustrations.
- Keep text inside graphics large enough to read on narrow screens.
- Include meaningful `alt` text where assets are embedded.
- Avoid duplicating information that is already available as selectable README text.
- Use relative repository paths for repository-owned assets.
